<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('blockchainsdk_wallets', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('user_id')->nullable()->index();
            $table->string('network', 32)->index();
            $table->string('address', 128)->index();
            $table->text('private_key');
            $table->text('public_key')->nullable();
            $table->text('mnemonic')->nullable();
            $table->decimal('balance', 24, 8)->default(0);
            $table->boolean('is_active')->default(true);
            $table->timestamps();

            $table->unique(['network', 'address']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('blockchainsdk_wallets');
    }
};